This is a proof of concept script, to password protect
a shell script, it will aternate the password from the
default password of tcp65535 to udp65535; use at your own risk
because sed is being used and will modify all occurrences of
tcp and vice versa.
